{
  "name": "LocaleBridge",
  "version": "1.0.0",
  "description": "A CLI tool for managig localization keys and values for i18nexus",
  "main": "src/index.js",
  "author": "Catherine Östlund (ic3dragon)",
  "license": "MIT",
  "private": true,
  "scripts": {
    "lint": "eslint . --fix",
    "bridge": "node src/index.js",
    "LocalBridge": "node src/index.js",
    "delete-test": "yarn run bridge D -f common.json -ns common -l en",
    "import-test": "yarn run bridge i -f common.json -ns common -l en"
  },
  "dependencies": {
    "axios": "^1.6.8",
    "commander": "^12.0.0",
    "eslint": "^8.57.0"
  }
}